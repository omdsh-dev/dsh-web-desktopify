# 壳源码内嵌 CLI

CLI 需要 `go install` 后脱离仓库运行，但壳构建需要完整源码树。因此壳
构建输入以 go:embed 内嵌在 CLI 二进制，运行时解出为独立模块根（动态
生成 go.mod，module 名复用仓库路径使壳内 import 解析到本地子包）再
`go build`。后果：CLI 不依赖仓库 checkout；壳模块绑定 FQN 稳定为
`github.com/omdsh-dev/dsh-web-desktopify/pkg/shell/...`。
