# 网关反代与注入

后端端口由 OS 分配（随机），重启会变；页面 origin 变化会导致认证失效，
且 wails runtime 需要注入。因此壳内网关（gateway）以稳定端口反代后端，
并在 index.html 强制注入 wails runtime.js 与共享 localStorage bridge；
窗口只加载网关地址，全壳只有一个网关实例（由入口创建并接上 wails）。
后果：后端随机端口重启不影响页面 origin；SSE 长连接经 ReverseProxy 原生
透传；认证 token 由浏览器 URL 携带，网关转发时保留 query。
